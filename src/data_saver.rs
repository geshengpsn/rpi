enum Signal {
    Start,
    End,
}

struct DataSaver {

}

impl DataSaver {
    fn new(file_path: &str) -> Self {
        // let vw = VideoWriter
        todo!()
    }

    fn write() {}
    
    fn close() {}
}